# WordMaster 快速测试

核心学习流程使用可注入业务时钟。自动化测试和测试实验室都不需要等待真实日期变化，也不会连接生产数据库。

## 自动化测试

```bash
python3 -m venv .venv
.venv/bin/pip install -r backend/requirements-dev.txt
cd backend
../.venv/bin/pytest -q
```

测试使用临时 SQLite 数据库，覆盖新学、错词重学、强化、五次复习计划、重复提交、午夜跨天、逾期补学和旧库迁移。

AI 测试全部使用模拟 MiniMax 响应，不会调用真实 API 或消耗套餐。覆盖：

- 脏释义清洗与“英文单词 + 350ms + 精简中文”播报脚本；
- 同词同义复用、统一学习内容解析和原始词条不可变；
- 持久化文字、图片、语音任务及 30% 额度保留线；
- 负反馈期间保留旧版，新版完整后原子切换；
- 图片问题只重做图片，词义问题重建完整记忆包；
- 旧 AI 字段迁移为版本 1，旧例句语音不会被当作中文播报；
- 完整 ZIP 备份的媒体 SHA256 校验与版本关系恢复。

## 时间实验室

```bash
docker compose -f compose.test.yaml up --build
```

打开 `http://localhost:8081`，注册测试用户后进入“时间实验室”。可以：

- 设置任意日期和时间，或按分钟、按天推进；
- 跳转到下一次复习日；
- 载入全新三词组、中途退出、刚完成强化、积压 16 天、10 词待复习等场景；
- 查看每轮原计划日期、逾期天数、前序阻塞和可复习状态。

测试模式使用容器内的 `/tmp/wordmaster-test.db`，不挂载生产数据卷。后端仅在 `ENV=test` 且数据库路径明确包含测试或临时目录时注册时间控制接口，否则启动会直接拒绝。

## 固定规则

- 新学、强化和每次复习的第一轮覆盖全部目标单词。
- 后续轮次只出现上一轮答错的单词。
- 每轮必须答完全部目标词才能推进。
- 强化完成后生成第 1、3、7、15、30 天五次复习。
- 逾期日期保持不变，同一学习组按最早未完成轮次依次补学。

## AI 静默任务

- 测试环境不会启动静默 Worker。
- 生产媒体目录默认为 `/app/data/ai-media`。
- MiniMax 中国区默认使用 `https://api.minimaxi.com`、`image-01` 和 `speech-2.8-turbo`。
- 普通任务在剩余额度不高于 30% 时暂停，反馈替换任务使用 20% 保留线。
- 完整备份在“数据备份”页面导出 ZIP；旧 JSON 备份仍可导入，缺失媒体会自动重新排队。
